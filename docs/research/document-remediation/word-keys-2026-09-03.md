# The Word keys tell the truth — r28 and r32 were stale keys, not lost headings

**Date:** 2026-09-03
**Predecessors:** `blind-test-2026-08-29-results.md` (the original claim),
`second-corpus-results.md` (the heading rule that made the keys stale),
`AGENTS.md` standing note "counts.headings on a Word row is a FIDELITY expectation".

> **Corrected the same day.** The r23 / r30 finding below — "a real heading the
> conversion drops" — was the instrument again. See "Second correction" at the end;
> the sections between are left as written, because the record of being wrong is
> the point of the record.

## The claim being retracted

Two count disagreements stood on the record since the 2026-08-29 run: r28 (key 13,
delivered 12) and r32 (key 5, delivered 4), explained as "headings that do not survive
conversion". A later note in `AGENTS.md` said that explanation had never been verified.
This document verifies it, with the key author's own code path rather than another probe.

## Method

`author-real-keys.mjs`'s `readDocx` heading loop, run over the nine `r*` Word sources
under five variants of its rule, one per commit the rule has had plus the one this
change adds. Per-paragraph tables (index, style id, resolved level, text length, drawing
count — no text) identified the differing paragraph on every row that moved. The
delivered side is the last full run before this change (`2026-09-02T02-47-41-113Z`),
read with qpdf.

| variant | rule | commit |
|---|---|---|
| A | `HeadingN` style-name match, no `w:basedOn`, no empty skip | `5ad8352` (keys authored) |
| B | A + skip paragraphs with no `<w:t>` text | — |
| C | outline level (direct `w:outlineLvl`, or style level through `w:basedOn`), no empty skip | — |
| D | C + skip paragraphs with no `<w:t>` text | `56a08b2` ("today's rule") |
| E | C + skip paragraphs with no text AND no described drawing | this change |

## The four-variant table

| row | A | B | C | D | E | key | delivered | verdict |
|---|---|---|---|---|---|---|---|---|
| r23 | 35 | 35 | 36 | 36 | 36 | 35 | 35 | key stale; delivery one short (TOC heading) |
| r25 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | agree |
| r27 | 11 | 11 | 11 | 11 | 11 | 11 | 11 | agree |
| r28 | 13 | 9 | 17 | 11 | 12 | 13 | 12 | key stale; delivery right |
| r30 | 8 | 8 | 9 | 9 | 9 | 8 | 8 | key stale; delivery one short (TOC heading) |
| r31 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | agree |
| r32 | 5 | 4 | 5 | 4 | 4 | 5 | 4 | key stale; delivery right |
| r33 | 13 | 13 | 14 | 13 | 13 | 13 | 13 | agree |
| r34 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | agree (see note) |

Every key reproduces byte-for-byte under variant A and only under A. The keys were
authored at `5ad8352` under A; the rule became D at `56a08b2`, whose `--corrections`
run was `--only=n`, so the `r` cohort was never re-read. Four of nine keys were stale.

## Per row, the paragraph that moved

- **r32** — paragraph 27 of 185: `Heading2`, zero text, one `<w:br w:type="page"/>`.
  Variant A counts it (it has the style); D and E do not (it says nothing). LibreOffice
  imports a lone page break as a paragraph property, so the flat ODF heading is empty
  and `removeEmptyHeadings` deletes it. Delivered 4 is correct.
- **r28** — paragraph 47 of 95: `Heading2`, zero text, one `<w:drawing>` whose
  `wp:docPr` carries a `descr`. Variant D calls it empty (no `<w:t>`); the product does
  not: the description lands in `<svg:desc>` inside the `<draw:frame>`, survives the
  tag-strip, and the export carries it as `/H2` over a `/Figure` with that `/Alt`.
  Delivered 12 is correct, and re-authoring under D would have set key 11 against
  delivered 12 — `invented-structure`, fatal at any weight, against a converter that had
  carried the author's own image. Variant E mirrors the product's test. (The other
  paragraphs A counts and D does not — 45, 54, 76 — are `Heading2` with nothing in them;
  paragraphs 11, 84–86 are a custom style based on `Heading2` that A cannot see.)
- **r23** — paragraph 22 of 254: `TOCHeading`, based on `Heading1`, 8 characters.
  **r30** — paragraph 4 of 191: `TOCHeading`, based on `Heading1`, 17 characters.
  Both are headings by OOXML's definition (outline level 1 through `w:basedOn`), and
  both are missing from the delivery: the delivered level sequences run
  `H1,H2,H3,…` and `H1,H1,H2,…` where the sources run `1,1,2,3,…` and `1,1,1,2,…`.
  `[V]` Converting r23 to flat ODF shows the mechanism: the flat ODF has 35 `<text:h>`
  and the TOC heading is the `<text:index-title>` of the table of contents, whose style
  `Contents_20_Heading` has `style:parent-style-name="Heading_20_1"` and
  `style:default-outline-level=""` — a `<text:p>`, not a `<text:h>`. **This is a real
  heading the conversion drops**, and the only one across the nine rows. It is recorded
  here as a product finding and appears in the scorecard as a non-fatal `counts-off` note
  on two probe rows. The key was not bent to match.
- **r33** — variant C counts 14 and D 13: one empty `Heading2` (no page break, no
  drawing). Deleted by the product, correctly.
- **r34** — 0 under every variant and 0 delivered. A raw qpdf read of the delivered PDF
  shows 18 elements typed `/H1` and `/H3`; the file's `/RoleMap` maps both to `/P`,
  because the source's custom paragraph styles are literally named "H1" and "H3" and
  carry no outline level. The product resolves the role map (`Inspect.standard()`) and
  reports 0; the ad-hoc probe that did not resolve it reported 18. The same instrument
  defect the PDF key author fixed at `56a08b2`, reproduced by a throwaway script — which
  is why the method above uses the author's code path.

## What changed

1. **The author's emptiness test mirrors the product's.** `author-real-keys.mjs` and
   `verify.mjs` (its twin, by the independence rule) count a heading paragraph that has
   `<w:t>` text OR a drawing (`wp:docPr` / `v:imagedata`) carrying a non-empty `descr` or
   `title`. The comment names r28 as the document that showed the two tests disagreeing.
2. **`corrections.json` gains `counts` corrections for r23, r28, r30 and r32**, kind
   `instrument-defect`, evidence naming the rule and both commits. Key files and hashes
   are unchanged. Re-running the cohort also re-read the `r` PDF rows under the current
   reading: no count moved; r13's `needs` gains the `4.1.2` the form-field rule added
   after the cohort's last corrections run (the product already voices it).
3. **Product: an undescribed image-only heading is demoted to a paragraph.** The plan
   predicted the old code would DELETE it (strip the tags, find nothing, drop the
   subtree). `[V]` It did not: a flat ODF embeds every image as base64 inside
   `office:binary-data`, the tag-strip left that payload standing as "text", and the
   first blind run of `w20` delivered the undescribed image-only heading as a heading —
   three against a key of two, `invented-structure`, fatal. So the defect was the
   opposite of the prediction: a heading announced over a figure with no name. Two
   things had to change, each measured before it was kept: `readableText` discounts
   `office:binary-data` before asking whether anything is left (and `firstHeading` uses
   it too, so an image-only first heading can never become a base64 title); and the
   demotion drops the STYLE NAME as well as the outline level, because a `<text:p>`
   still styled `Heading_20_2` was exported as `/H2` anyway — the style carries
   `style:default-outline-level="2"` and the importer reads a paragraph's level from its
   style when the tag does not say. Exporting the corrected flat ODF by hand gave
   `/H1 1, /H2 1, /Figure 2`. A described image-only heading stays a heading, as it
   always did. Tests in `flat-odf.test.ts` carry both shapes with the base64 in place.
4. **`w20-image-only-heading`** plants both shapes, `core`: a real H1, a `Heading2`
   whose only run is an undescribed drawing, a `Heading2` whose only run is a described
   drawing. Key: headings 2, figures 2, needs `1.1.1`. Three headings means the
   converter kept an empty heading; one means it dropped a described image; one figure
   means the demotion is still a deletion.

## The r cohort, before and after

| row | key | corrected to | delivered | scorecard before | scorecard after |
|---|---|---|---|---|---|
| r23 | 35 | 36 | 35 | hit (by coincidence) | `counts-off`, non-fatal — product finding |
| r28 | 13 | 12 | 12 | `counts-off` | hit |
| r30 | 8 | 9 | 8 | hit (by coincidence) | `counts-off`, non-fatal — product finding |
| r32 | 5 | 4 | 4 | `counts-off` | hit |

Two notes leave the scorecard and two arrive, and the two that arrive are the two that
were true all along.

## Blind run

Two full runs of 151 rows, no subset (the runner has none).

**Run 1, `2026-09-03T20-58-41-520Z`** — the corrected keys and the first version of the
demotion (style name kept, binary data not discounted). One fatal: `w20` delivered
**3** headings against a key of 2, `invented-structure`. That is the measurement that
overturned the plan's prediction (see "What changed", 3) and is the reason the row
exists. r28 and r32 were already off the notes list; r23 and r30 were on it.

**Run 2, `2026-09-03T21-15-47-185Z`** — rebased on `f9fa041`, both product corrections
in, every gate green on the same tree (`npm test` 2121, lint, typecheck, build, chaos,
`build:documents`, `test:documents` 63, `test:hydration` 49).

| | run 1 | run 2 | DoD |
|---|---|---|---|
| fatal findings | 1 | **0** | 0 |
| invented claims | 1 | **0** | 0 |
| silent gaps | 0 | **0** | 0 |
| drift | 0 | **0** | 0 |
| disposition (core) | 46/46 | 46/46 | |
| door | 11/11 | 11/11 | |
| r28 `counts-off` | no | **no** | gone |
| r32 `counts-off` | no | **no** | gone |
| r23 `counts-off` (key 36, reported 35) | yes | yes | product finding, non-fatal |
| r30 `counts-off` (key 9, reported 8) | yes | yes | product finding, non-fatal |
| w20 headings / figures | 3 / 2 | **2 / 2** | 2 / 2 |
| w20 `1.1.1` voiced, gap listed | yes | **yes** | yes |
| key corrections | 58 across 31 documents | 58 across 31 | |

Scorecard, run 2: "every promise held". The 14 missing punch items and 2 unexpected
are the standing 7.21.4 / p22 / w12 notes, unchanged from the 2026-09-02 run.

## Noticed, not done

- ~~**The TOC heading loss is fixable in `flat-odf.ts`**~~ — promote a
  `<text:index-title>`'s paragraph to `<text:h text:outline-level="1">` when its style
  descends from a heading style. **Withdrawn**: the evidence this note said had to be
  gathered was gathered the same day and points the other way. The TOC title's style
  carries its own outline level 9 — the author's declaration that it is NOT a heading —
  and Word's outline, Word's PDF export and LibreOffice all honour it. Promoting it would
  invent structure. The decision is to fix the instrument and leave the product alone;
  see "Second correction".
- `score.ts`'s asymmetry (more headings than the key is fatal at any weight) stays. It
  is why the key had to be right rather than the rule loosened.

## Second correction (2026-09-03, later the same day): the TOC title was body text

### What the first pass got wrong

The r23 / r30 row above reads: "Both are headings by OOXML's definition (outline level 1
through `w:basedOn`)". That was read from the paragraph — `w:pStyle TOCHeading`, no direct
`w:outlineLvl` — and from the style's `<w:basedOn w:val="Heading1"/>`. It was not read
from the rest of the style. `[V]` `word/styles.xml` in both documents:

| document | style | `w:basedOn` | style's own `w:outlineLvl` | paragraph (index) | direct level |
|---|---|---|---|---|---|
| r23 | `TOCHeading` | `Heading1` | **9** | 21 | none |
| r30 | `TOCHeading` | `Heading1` | **9** | 3 | none |

`w:outlineLvl` runs 0–8 for heading levels 1–9 and **9 is "Body Text"** — the value Word
writes when a style or paragraph is taken out of the outline. It is an explicit override:
a style that says 9 is not a heading whatever it descends from. That is exactly what
Word's own `TOC Heading` template style looks like (based on Heading 1 so it is styled like
one; level 9 so it is not one), and the paragraph is followed in both documents by a real
`<w:sdt>` TOC field (`TOC \o`, 35 and 8 `TOC1`–`TOC3` entries with `PAGEREF` hyperlinks).

Three independent consumers agree with the override:

- **Word's navigation pane** does not list a level-9 paragraph.
- **Word's own PDF export** writes it as body text, not `/H1`.
- **LibreOffice's import** produces `<text:index-title>` whose style
  `Contents_20_Heading` has `parent-style-name="Heading_20_1"` and
  `default-outline-level=""` — the ODF spelling of "based on a heading, not a heading"
  — and the paragraph inside it is a `<text:p>`. The first pass saw the `<text:p>` and
  called it a loss; it was a faithful transcription of level 9.

The author's `styleLevel` regex was `/<w:outlineLvl w:val="([0-8])"\s*\/?>/`. A 9 never
entered `own`, so `TOCHeading` had no level of its own as far as the walk could see, the
walk followed `w:basedOn` to `Heading1`, and the title was counted as an H1. `verify.mjs`'s
`countHeadings` — the author's twin by the independence rule — had the same range. Two
instruments, one gap, one wrong reading each: 36 and 9 against sources that carry 35 and
8 headings by their authors' own declaration.

### What the product delivered, read with qpdf

`.doc-blind-test/delivered/r23.pdf` and `r30.pdf` from run `2026-09-03T21-15-47-185Z`,
structure tree walked by qpdf `--json=2` with the `/RoleMap` applied:

| | r23 | r30 |
|---|---|---|
| `/Document` children | `/H1` 1, `/H2` 5, `/H3` 29, `/TOC` 1, `/Table` 4, `/L` 20, `/P` 67 | `/H1` 2, `/H2` 4, `/H3` 2, `/TOC` 1, `/Table` 15, `/P` 48 |
| `/TOC` children | `/Caption` 1, `/TOCI` 35 | `/Caption` 1, `/TOCI` 8 |
| `/Caption` child | `/Contents Heading` → RoleMap → `/P` | `/Contents Heading` → RoleMap → `/P` |
| headings after RoleMap | **35** | **8** |
| veraPDF UA-1 | compliant | compliant |

The title sits where PDF/UA wants a TOC title — as the `/Caption` of the `/TOC` — and its
custom role maps to `/P`. `Finish.renumberHeadings` is identity on both (rank set
{1, 2, 3}). The product read the document the way its author wrote it.

### The decision

**Fix the instrument; do not change the product.** Promoting the `<text:index-title>`
paragraph to a heading in `flat-odf.ts` — the "Noticed, not done" item above — would tag
as a heading a paragraph whose author declared it body text. That is the r34 class of
invented structure (custom styles literally named "H1" and "H3" with no outline level,
correctly delivered as `/P`), and the corpus's asymmetry exists to catch exactly it.

### What changed

1. **`author-real-keys.mjs` and `verify.mjs`** read `w:outlineLvl` as `[0-9]`; a 9 on a
   style is stored as `null` so the `w:basedOn` walk stops there ("this style has
   decided"), and a direct 9 on a paragraph is body text too. The lookup is
   `styleLevel.has(id) ? styleLevel.get(id) : HeadingN-fallback`, because a `??` over a
   stored `null` would have fallen through again. Comments name r23 / r30 and the three
   consumers that agree.
2. **`corrections.json`**: the r cohort was re-read with the author's own flags
   (`--corrections --only=r`). r23 and r30 now read 35 and 8, equal to the locked keys,
   so no correction is emitted and the two 2026-09-03 entries are gone — the overlay
   records where the instrument disagrees with a locked key, and it no longer does. The
   evidence string on the surviving Word-count corrections (r28, r32, r34) now records the
   level-9 rule and the withdrawal. Every other entry is byte-identical: 44 corrections
   across 19 real documents in the cohort, r13's `4.1.2` included. Key files and hashes
   unchanged.
3. **`w21-toc-title-body`** (`core`): `stylesXml` gains a `bodyLevelStyle` option — a
   style `basedOn Heading1` with its own `outlineLvl 9`, written after `w:basedOn` as Word
   writes it — kept separate from `customHeading`, which is deliberately level-less to
   test inheritance. Body: `Heading1`, a `TOCHeading` paragraph, `Heading2`, a paragraph.
   Key: headings 2, needs `[]`. Three delivered headings means the converter promoted a
   paragraph its author demoted (`invented-structure`, fatal); one means it lost a real
   one. No TOC field is planted: the rule under test is the level override, and the field
   shape is covered by the two real documents.
4. **`AGENTS.md`**: the outline-level rule gains the level-9 clause; the standing note on
   r23 / r30 now says what they were.

### Blind run

**Run 3, `2026-09-03T22-10-24-265Z`** — 152 rows (151 plus `w21`), no subset, on the
tree carrying the instrument fix and the withdrawn corrections, every gate green on the
same tree (`npm test` 2121, lint, typecheck, build, chaos, `build:documents`,
`test:documents` 63, `test:hydration` 49). No product code changed.

| | run 2 | run 3 | DoD |
|---|---|---|---|
| fatal findings | 0 | **0** | 0 |
| invented claims | 0 | **0** | 0 |
| silent gaps | 0 | **0** | 0 |
| drift | 0 | **0** | 0 |
| counts off | 2 (r23, r30) | **0** | r23 / r30 gone |
| disposition (core) | 46/46 | **47/47** | |
| door | 11/11 | 11/11 | |
| r23 headings (key 35) | 35, `counts-off` against the 36 overlay | **35, hit** | |
| r30 headings (key 8) | 8, `counts-off` against the 9 overlay | **8, hit** | |
| w21 headings / TOC title | — | **2** / `Contents Heading` → RoleMap → `/P` | 2 / `/P` |
| key corrections | 58 across 31 documents | **56 across 29** | two withdrawn |

`w21` delivered, read by qpdf: `/Document` → `/H1` 1, `/Contents Heading` (→ `/P`) 1,
`/H2` 1, `/Standard` (→ `/P`) 1. The converter carried the author's declaration exactly:
the style name survives as a custom role, the role maps to `/P`, and no heading was
invented over it. Scorecard: "every promise held". The 14 missing punch items and 2
unexpected are the standing 7.21.4 / p22 / w12 notes, unchanged since 2026-09-02.

| row | key | 2026-09-03 first pass | rule now | delivered | scorecard |
|---|---|---|---|---|---|
| r23 | 35 | 36 (`[0-8]`, fell through to `Heading1`) | 35 (level 9 stops the walk) | 35 | hit |
| r30 | 8 | 9 (same) | 8 (same) | 8 | hit |
| w21 | 2 | — | 2 | 2 (`/H1`, `/P`, `/H2`, `/P`) | hit |
