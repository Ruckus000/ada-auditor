# Stage 1 Round 12 Implementation Plan — a third epoch to cut abstention

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce round 10's abstention at the registered operating point (25.1 %, gate ≤ 10 %) by continuing its training for one more epoch, on the measured fact that half the abstained rows are decisions the model gets right but scores below the threshold.

**Architecture:** One variable. `adapter-r12` = `adapter-r10` resumed (`--adapter-path out/stage1/adapter-r10`) for one further epoch over `sft-r10` (4,942 iters, default learning rate, same flags). Prediction with `--scores`, the round-10 evaluation and coverage method, the registered threshold rule from round 11, and the error-row audit only if new un-audited errors appear.

**Tech Stack:** as round 10. `mlx_vlm.lora` accepts `--adapter-path` to resume.

**Spec:** `2026-09-18-stage1-round11.md` (threshold rule, operating point), `heading-stage1-r11-2026-09-18-results.md` (operating-point-r10.json: threshold 0.9933, coverage 0.749, 383 abstained).

## Global Constraints

- Round 10's Global Constraints apply verbatim (split, no test, label sources, level policy, c5 excluded, 15 h gate, commit discipline).
- **Measured basis (registered):** of r10's 380 abstained rows at 0.9933, 187 score in [0.95, 0.9933) and 172 of those are correct; 58 score below 0.7 and 43 of those are correct. The abstained mass is mostly correct-but-underconfident, which a further epoch can sharpen; the risk is overconfidence on the wrong ones, guarded by the FP bound.
- **Registered before training:**
  - Data: `sft-r10` unchanged (sha 6e1e7db6…). Config: r10's command with `--adapter-path out/stage1/adapter-r10 --iters 4942`; expected ~7 h; 15 h gate at step 100.
  - Prediction (r10 → r12 on labels-audited-r10, r10v rates, threshold rule unchanged): coverage at the rule's threshold rises from 0.749 to ≥ 0.82 (abstention ≤ 18 %) with the rule still satisfied (accuracy lower bound ≥ 0.98, FP upper bound ≤ 0.02); direct FP on all 1,525 ≤ 0.045; estimated FN ≤ 0.125; regular-weight recall ≥ 0.90.
  - Revert: if direct FP > 0.05 on audited labels, or the rule's threshold yields lower coverage than r10's, r10 stays the candidate.

---

### Task 1: Resume-train r12, predict, evaluate

- [ ] **Step 1: Launch** r10's training command with `--adapter-path out/stage1/adapter-r10 --iters 4942 --output-path out/stage1/adapter-r12`, log to `train-r12.log`; step-100 projection against 15 h; record.
- [ ] **Step 2: Predict** on validation with `--scores` → `pred-validation-r12.jsonl`; 0 parse failures; test.spent absent.
- [ ] **Step 3: Evaluate** on labels-audited-r10 beside r10 (direct, estimated with r10v rates), recall by weight, coverage curve, and the threshold rule → `operating-point-r12.json`; registered prediction met or not.
- [ ] **Step 4: audit-r12v** only if r12 has ≥ 10 un-audited error rows (error rows only, round-8 method); coordinator judges; fold to labels-audited-r12; re-score r12 and r10; recompute both operating points.
- [ ] **Step 5: Record** `heading-stage1-r12-2026-09-18-results.md`, stop decision (a) r12 candidate with its operating point / (b) revert to r10, and the roadmap Stage 1 status line. Commit.

**Not in this plan:** new facts, new labels, product wiring, the test split.
